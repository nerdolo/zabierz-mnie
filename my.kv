#:kivy !

WindowManager:
    StartScreen:
    SelectTypeScreen:

<StartScreen>:
    name: "start"

    BoxLayout:
        orientation: 'vertical'
        Button:
            text: "Zabierz mnie stąd"
            on_release:
                app.root.current = "selecttype"
                root.manager.transition.direction = "left"


<SelectTypeScreen>:
    name: "selecttype"

    BoxLayout:
        orientation: 'vertical'
        Button:
            text: "Poza dachem"
            on_release:
                app.root.current = "main"
                root.manager.transition.direction = "right"
        Button:
            text: "Pod dachem"
            on_release:
                app.root.current = "main"
                root.manager.transition.direction = "right"

<SelectPlaceScreen>:
    name: "selectplace"

    BoxLayout:
        orientation: 'vertical'
        Button:
            text: ""
            on_release:
                app.root.current = "main"
                root.manager.transition.direction = "right"
